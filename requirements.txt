# Core framework (Django 5.2 LTS line, current DRF)
Django>=5.2,<5.3
djangorestframework>=3.15,<3.17

# Config / DB (host-agnostic: DATABASE_URL parsing, Postgres in prod)
dj-database-url>=2.2,<3.0
psycopg[binary]>=3.2,<4.0

# Image processing: thumbnails + DRF ImageField upload validation (P3).
# The Vercel Blob client is stdlib-only (urllib) — no HTTP dependency added.
Pillow>=10,<12

# Prod WSGI server
gunicorn>=23.0,<24.0
